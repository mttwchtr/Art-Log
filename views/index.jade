extends layout

block content
  h1= title
  p Welcome to #{title}
  each artist in artists
    a(href= '/artist/' + artist.artist_id) #{artist.name}
    br
    img(src= artist.url, width='100px')
    p= artist.years

